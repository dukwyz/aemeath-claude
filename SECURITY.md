# 安全说明

## 端口监听

爱弥斯在本地监听以下端口：

| 端口 | 用途 | 绑定地址 |
|------|------|----------|
| 9527 | HTTP API（接收 Claude Code 事件） | `127.0.0.1` |
| 9528 | MCP 服务器（双向通信） | `127.0.0.1` |

两个端口都绑定在 `127.0.0.1`（localhost），**仅本机可访问**，不暴露给外部网络。

## 权限

- 不收集任何用户数据
- 不访问网络（除本地 Claude Code 通信）
- 不修改系统文件
